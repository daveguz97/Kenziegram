let express,multer,fs;_0d0‍.w("express",[["default",["express"],function(v){express=v}]]);_0d0‍.w("multer",[["default",["multer"],function(v){multer=v}]]);_0d0‍.w("fs",[["default",["fs"],function(v){fs=v}]]);



const app = express();

const port = process.env.PORT || 3000;

// app.set("view engine", "ejs");

const path = "./public/uploads";

app.get("/", (req, res) => {
  fs.readdir(path, (err, items) => {
    _0d0‍.g.console.log(items);
    res.render(`index`);
  });
});

app.listen(port, console.log(`Server running on port ${port}...`));
